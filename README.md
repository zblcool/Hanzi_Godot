迁移前的项目是一个“汉字游戏启动器”，里面有两个可玩的网页3D原型游戏：
我们现在先专注在字海上

1. `字海残卷`
- 实时动作生存 / roguelite
- 核心是“偏旁收集 -> 自动合字 -> 汉字技能 -> 更高阶词技”

2. `仓颉之路`
- 类 Slay the Spire 的卡牌爬塔 / deckbuilder
- 核心是“偏旁、汉字、语义组合”直接进入战斗和构筑

说明：下面保留了迁移前 web 原型的玩法与内容记录，方便继续对照设计目标；但仓库当前实现已经切到 Godot，工程入口、目录结构与导出方式请以本 README 的“当前整体结构”“启动器 / 首页现状”“工程结构”“Vercel 预览部署”为准。

## 迁移追踪
- 当前仓库的主目标是把原来的 `hanziHero` web 原型逐步迁到 Godot，并保持玩法、UI 层级与内容方向持续对齐。
- 迁移状态、已完成项和待补项请看 [`MIGRATION_CHECKLIST.md`](./MIGRATION_CHECKLIST.md)。

## 当前整体结构
- 技术上当前是 `Godot 4.6.1` 项目，运行入口是 `project.godot -> res://scenes/app/launcher.tscn`
- 当前仓库主线已经落地 `字海残卷` 的 Godot 迁移链路：`启动器 -> 字海二级菜单 -> 3D 战斗`
- `仓颉之路` 目前已经有独立的启动器 portal overlay，会预览 `起笔登塔 / Start Climb`、`运行控台 / Run Controls`、`卡牌字库`、`合字图谱`、`遗物架`、`战后抉择 / Post-Battle Flow`、`塔路导览`，并把 source 页面的 `重新开局 / 查看牌组 / 返回启动器` 操作条、`Floor / HP / Deck / Relics / Gold` 顶部状态壳，以及三路线塔图的 route shell 一起迁回前台预览；portal 页脚现在也保留了 `玩家名帖 / Player Sigil` 入口，方便不退出这层就直接改默认署名；其中 `查看牌组 / Open Deck` 现在还能在 portal 里直接展开一份轻量牌组 / 遗物样张，`战后抉择 / Post-Battle Flow` 现在也能切换查看常规拿牌、跳过保薄与精英 / 卷主遗物跟进三条链路，`塔路导览` 里的三路线塔图本身也能点按节点展开 battle / event / rest / shop / elite / archive / treasure / boss 的 source 风格节点聚焦说明，并直接带出对应的拿牌 / 路书 / 稳线跟进标签；当前聚焦节点还会再抽出一条轻量 `路线丝带 / Route Ribbon`，把这一步前后通常接续的 climb 节拍和 `节点 -> 关键决策 -> 后续偏转` 一起并排读出来，并额外补上一组紧凑 `估值偏转 / Value Shift` 卡，点明当前 deck 倾向、Gold 节奏与已持 relic 会怎样改写这格的价值；楼层之间也改成了更接近 source 塔图读法的层间连线 overlay，并额外补上一组 `主脉 / 转笔 / 收束 / 聚焦` 样张读图图例；现在还会按当前聚焦节点额外演示一段轻量步态，把 `已走 / 已开 / 未亮` 三种塔图状态先挂回壳层，并额外保留一条紧凑 `已走笔迹 / Path Trail`，让焦点推进到下一排时刚压过的样张路线不会立刻丢失，再把直连下一排会亮起的房间抽成一组 `下一排接续 / Next-Row Handoff` 说明卡，补上它们更像接战后拿牌、路书事件、稳线修整还是遗物偏转的提示；这些已开房间除了紧凑的节点节拍样张外，现在还会直接带出一枚 `路线读法 / Route Read` cue，并额外补上一行轻量 `估值偏转 / Value Shift` hook，让未切主聚焦前也能先读这格更像压深、补短、存势、保薄兑现还是会被当前 deck / relic / Gold 节奏改写估值，而且也能直接点按，把主聚焦、路线丝带和联动预览一起切到对应房间；同一栏里现在也多挂回一份代表性的 `歇息小站 / Quiet Stop`、`删改抉择 / Archive Edit`、`可见遗物 / Visible Relic`、`商店重配 / Shop Reallocation`、`路书对照 / Route Ledger`、`静室师言 / Tutor In Silence`、`抄手交易 / Copyist Bargain` 与 `边注赌约 / Margin Wager` 样张，让 source 的 rest 房怎样在 `静养 / Recover` 与 `蓄势 / Prepare` 之间改写下一场起手、archive 房怎样在修薄与誊抄关键件之间改写牌组形状、treasure 房怎样让可见遗物直接扭动后续估值，以及商店 / 事件怎样继续改写预算和后手都能提前读到；`Tower Guide` 这一栏现在还会额外带着一份紧凑的 `Restart Run / Open Deck / Back To Launcher` 与 `Floor / HP / Deck / Relics / Gold` 运行壳，`Open Deck` 也能在塔图上方直接展开，不必切回单独的 `Run Controls` 说明；但还没有在这个仓库里落成独立可玩的 Godot 场景
- Web 导出通过 `./scripts/export_web.sh` 生成到 `build/index.html`，`vercel.json` 负责部署入口
- 当前默认开发流程已经切到 `develop -> staging -> main`：`develop` 承接日常开发与自动化，`staging` 只做线上验收，`main` 保持稳定主干
- `README.md`、`CHANGELOG.md`、`CONTRIBUTING.md` 持续同步当前迁移状态

## 启动器 / 首页现状
- 首页当前由 Godot `Control` 场景实现，视觉方向仍然是主机首页 / 选游戏入口
- 首页视觉标题仍是 `汉字游戏启动器`，并保留两个游戏入口卡片与 `关于字海` 信息面板；Godot 工程本身在 `project.godot` 里的应用名是 `字海残卷`
- `关于字海` 面板现在会明确说明这条线面向的玩家 / 学习者、为什么先保留网页 demo 传播路径，以及下一步产品化判断，前台叙事更贴近原 `hanziHero` 启动器
- 首页现在额外有一张“最近更新”进度卡，会直接概括当前迁移主线、最近补齐的前台能力，以及仍待追赶的启动器缺口
- “最近更新”进度卡现在还可以直接展开启动器内置的更新历史面板，回看最近几次 Godot 迁移里程碑，不必离开首页
- 启动器、`关于字海` 面板和 `字海` 二级菜单现在都支持 `夜墨 / 纸墨` 主题切换，并会把当前选择记到本机下次启动继续沿用
- 启动器与 `字海` 二级菜单现在也支持运行时 `中 / EN` 切换，并会沿用同一份本机语言偏好；战斗 HUD 的双语跟进仍在迁移清单里单独追踪
- 背景不再依赖旧的 `Three.js` 页面，而是在 Godot 内直接绘制漂浮汉字与几何氛围层
- 有微信 / iOS 的视觉引导：
  - 微信里提示尽量用系统浏览器打开
  - iOS 提示 `分享 -> 添加到主屏幕`
- 点击 `字海残卷` 后，会先进入真正的二级菜单 `scenes/app/zihai_menu.tscn`
- 角色选择确认后，才进入 `scenes/battle/zihai_battle.tscn`
- `仓颉之路` 目前不再只是静态占位卡，而是有独立 portal overlay 预览 source 里的入口、运行控台、卡牌、合字、遗物、战后奖励链路、节点与敌意样本；入口页还补上了可点按回应的对峙舞台、source 风格的 `重新开局 / 查看牌组 / 返回启动器` 操作壳预览、三路线塔图 route shell、本地 `3D 特效` 开关、同层可用的 `玩家名帖 / Player Sigil` 入口、可点开的轻量 `查看牌组 / Open Deck` deck sheet、可切换的战后 reward / skip / relic 链路样张、可点按展开的节点聚焦说明，以及代表性的 `歇息小站 / Quiet Stop`、`删改抉择 / Archive Edit`、`可见遗物 / Visible Relic`、`商店重配 / Shop Reallocation`、`路书对照 / Route Ledger`、`静室师言 / Tutor In Silence`、`抄手交易 / Copyist Bargain` 与 `边注赌约 / Margin Wager` 样张；`塔路导览 / Tower Guide` 里的路线节点现在还会在点按后高亮对应样张路线，并拉出与该节点关联的战后 / 删改 / 遗物 / 商店 / 路书 / 歇息 follow-through 卡片，顺手标出它更像接战后拿牌、可见遗物改路、商店重配还是稳线修薄节拍，并额外抽出一条轻量 `路线丝带 / Route Ribbon`，把当前房间重新嵌回代表性的 climb 节奏与 `节点 -> 决策 -> 跟进` 节拍里，同时再补上一组紧凑 `估值偏转 / Value Shift` 卡，把 deck 倾向、Gold 预算和已持 relic 会怎样抬高这格一起讲清楚；更接近 source 塔图读法的层间连线 overlay、`主脉 / 转笔 / 收束 / 聚焦` 状态图例、一段按当前聚焦节点改写的 `已走 / 已开 / 未亮` 步态样张、一条会保留刚压过路线的紧凑 `已走笔迹 / Path Trail`，以及一组直接解释直连下一排房间会做什么的 `下一排接续 / Next-Row Handoff` 说明卡也都一起挂回壳层，并补上这些已开房间各自更像接拿牌、路书、稳线还是遗物偏转的提示；这些新亮起的下一排卡片现在也会直接带一条紧凑节点节拍、一枚 `路线读法 / Route Read` cue 与一行 `估值偏转 / Value Shift` hook，把 battle / event / rest / shop / elite / archive / treasure / boss 各自更像压深、补短、存势、保薄兑现还是会被当前 deck / relic / Gold 节奏改写估值的读法压回同一层里，并且能直接点按把主聚焦、路线丝带和联动预览推进到那一格；同一栏上方还会保留一份紧凑的 run shell，继续显示 `Restart Run / Open Deck / Back To Launcher` 与 `Floor / HP / Deck / Relics / Gold`，让塔图预览更接近 source 同屏读法；真正的 Godot 爬塔战斗仍待后续迁移接入

## 字海残卷（目前已经是较完整可玩原型）
### 核心玩法
- 实时移动、生存、自动攻击
- 收集偏旁后自动尝试合字
- 合出完整汉字后解锁技能
- 重复偏旁不会浪费，而是会给已有合字技能升级
- 当前已明确落地一批成字技能：`明` 日月追轮、`昌` 双日追轮、`休` 林息回波、`林` 前路木潮、`海` 墨潮波纹、`雷` 锁敌落雷、`岩` 震域落印、`忍` 残血忍意、`炎` 环喷火潮
- 每 `4` 波会触发一次 `字境相变`，在 `碑林 / 墨潮 / 雷纹 / 残卷` 之间轮换，并在玩家附近留下巨型古字印记
- 词技系统存在：部分高阶内容不只是单字，而是更高一层的“词”
- 现在词技不再是纯自动解锁，而是要在 `砚` 那里把满级合字进一步磨成词
- 当前词技已经覆盖 `明月 / 昌明 / 休养 / 林海 / 海啸 / 雷雨 / 忍心 / 炎潮`，让双日线也能继续接上定期明光追击与基础攻击回响。

### 角色
- 至少两个角色：
  - `书生`
  - `侠`
- `侠` 已经不是远程，而是近战剑士
- `侠` 手里有实体剑模
- 攻击时会真实挥剑
- 剑会随着 `刂` 刀字旁收集和成长而变大，而且已经被做得更夸张、更宽

### 敌人系统
当前已经不只是普通怪，谱系明显丰富了：
- `basic` 普通敌人
- `swift` 快速敌人
- `tank` 坦克敌人
- `archer` 弓箭手
- `assassin / 忍`
- `cavalry / 墨骑`
- `ritualist / 阵师`
- `elite`

这些新增敌人都不是简单换皮，而是有明确行为差异：
- `忍`
  - 中距离游走
  - 会短暂预警后沿直线突刺
  - 地上有偏紫色突进轨迹
- `墨骑`
  - 会从一侧冲锋到另一侧
  - 冲锋前地面会出现红色长方形路线预警
  - 命中会造成 1 秒晕眩
- `阵师`
  - 会在地图上放字阵 AOE 区域伤害
  - 有预警圈，之后转成持续危险区域
- `elite`
  - 不再只是高数值怪
  - 已经有技能池：
    - 爆圈
    - 一长串线性弹幕
    - 梅花形旋转弹幕
    - 类重骑的大型冲锋
  - 冲锋类技能也会在地上显示即将冲锋的路线

### 刷怪节奏
- 敌人出现波次整体被提前了
- 混编更早发生
- 每种兵种数量也更多了
- 战斗现在更早进入多兵种混合压力

### 地图 / 场景生态
地图里已经不只是空地：
- `宝箱`
  - 打开后给道具
- `树`
  - 大型实体
  - 人物走进去会半透明 / see-through
- `草丛`
  - 进入后短暂隐身
  - 敌人丢失目标
  - 后来又做了反滥用机制：
    - 草丛本身有恢复期
    - 玩家也有一个短的重入锁
    - 不能靠反复蹭草丛无限隐身
- `笔`
  - 临时属性增益
- `墨`
  - 群体 AOE
- `纸`
  - 现在不是圆形，而是长方形卷轴状的增益区域
- `砚`
  - 工具台，用来把满级合字进一步磨成词技
- `聚墨符`
  - 一次性回收战场上散落的字墨
- `疾书令`
  - 短时间提升移动与攻击节奏

### 地形
- 目前已经做了一版“轻量自然感地形”
- 不是完整 heightmap，也不是复杂高低差玩法
- 只是低成本的地面起伏 / mound / painted ridge，让场地不再死平
- 这是有意保守的第一步，为了避免性能和寻路复杂度爆炸

### 战斗反馈 / 视听
- 新合出汉字时会有明显的大特效：
  - 角色周围更重的笔画爆字
  - 屏幕上方会漂浮出更大的“合字成型”动画
- Godot 迁移版现在也会在 `合字成型`、`词技成型`、`字境相变`、卷主现身以及阶段性定卷时给出更大的居中揭示卡，让关键成长、卷主压阵和章节推进更接近 source 原型的“大字提示”节奏
- 首位卷主退散且场上字灵清空后，Godot 版现在会先弹出一个 `卷间缓冲` 继续关卡，要求手动确认后再续卷入深层，先把 source 里的房间之间停顿节奏补回第一步
- 在这次首个卷间停顿之前，`入卷前庭 / Entry Court` 现在还会先散出三枚 `封印` 目标点；要把它们逐个收束，卷间抉择才会真正打开，先把 source 那种“房间出口不只看清怪”的节奏补回第一刀
- 三枚 `封印` 收束之后，Godot 版现在还会先升起一枚可交互的 `奖` 卷间奖印；必须亲自走到奖印前，才会真正打开 `卷间抉择`，把 source 那种“先摸到房间奖印再结算”的节奏也补回第一刀
- 房间目标与卷间奖印现在也会给出一个屏幕边缘的实时引导牌，持续指向最近的 `封 / 奖` 目标点，把 source 里那种玩家附近的 `奖 / 封` 指引也补回了第一步
- 这个首个 `卷间缓冲` 现在也会给一组低风险 `奖励 / 异事 / 修整` 三选一：要么带走一个偏旁补给，要么给下一段挂上一层 `残卷回响` 掉落偏向，让压境敌群更容易回响残纸、精英额外吐出 `疾书令`，要么回气并补一段文笔提速，先把 source 的房间间歇抉择节奏补回第一刀
- `卷间抉择` 浮层现在还会先给出一个下一段预览，告诉你接下来是哪一波、会切到哪层字境，以及最可能混进来的关键威胁，先把 source 的房间预览卡补回一个低风险版本
- 这些卷间选择现在还会把一个轻量的“下一段修正”带进后续战斗：偏旁补给会抬高下一段残纸 / 战印掉落，`残卷回响` 会让压境敌群更常回响残纸、精英也能额外吐出一枚短时 `疾书令`，`歇笔修整` 则会在后续字潮推进时再补一小口气和文笔
- 首个 `卷间抉择` 现在也会轻推后续偏旁三选一：`简库拓片` 会更偏向你亲手带走的那两笔，`封钥借契` 会更偏向 `木 / 氵 / 田 / 月`，`守灯静读` 则会更偏向 `亻 / 心 / 木 / 月`，让房间抉择开始真正改写下一段 build
- Godot 版现在也会在首个 `卷间抉择` 结算后真正换进一个命名后的下一房间 `简库中庭 / Slip Archive`：树阵、卷架、碑刻、砚台、宝箱、补给点和地图迷雾都会按新房间重新铺开，而不是继续沿用入卷前庭的原布局
- `简库中庭 / Slip Archive` 里现在已经补上四道句阵守卫：`风雨同舟` 会给一段回气奖励，`海上生明月` 会唤出一名远程守句并额外给一段字墨收益，`山高水长` 会带来一名重装守句并扩大附近迷雾显形，`举头望明月` 则会补上一枚固定 `月` 偏旁，继续把 source 里的 guarded phrase event 和四类房间奖励往前接
- `简库中庭 / Slip Archive` 的卷主退散后，现在也会先亮起 source 风格的 `雷纹转钥封 / Storm Latch Seal`；要先逼出并击败 `简雷守将 / Slip Storm Marshal`，通往下一房间的卷间奖印才会真正显形
- 第二次卷间抉择现在也不再沿用首轮通用文案，而是会换成 `雷纹拓笔 / 伏雷换契 / 伏纹稳息` 三选一，并在结算后真正把战场重铺成 `雷纹内库 / Thunder Vault`：房间会改成更冷、更紧的雷碑 / 石架布置，奖励也会分成偏旁加速、雷势入场或纸域护势三种入场节奏
- `雷纹内库 / Thunder Vault` 里现在也补上了四道后室句阵守卫：`风驰电掣` 会额外给一段字墨，`电光石火` 会扩大附近迷雾显形，`云收雨霁` 会补一段回气，`雷霆万钧` 会补上一枚固定 `田` 偏旁，让第二段房间也开始有自己的句池节奏
- `雷纹内库 / Thunder Vault` 的卷主退散后，Godot 版现在也会换成一组终室专属卷间抉择 `终室备墨 / 渊页誓约 / 压关静息`，再把战场真正重铺成 `卷渊终室 / Abyss Sanctum`：这一段会把双偏旁补字、双势入场或高额回气分开处理，并把章节定卷的时点后推到终室卷主真正退散之后
- `卷渊终室 / Abyss Sanctum` 里现在也补上了四道终室句阵守卫：`绝处逢生` 会给一段更高的回气，`破釜沉舟` 会补一段额外字墨，`力挽狂澜` 会扩大终房附近迷雾显形，`薪火相传` 则会补上一枚固定 `火` 偏旁，让终室也开始有自己的句池节奏
- 第 20 波压测现在也会直接以 `卷渊终室 / Abyss Sanctum` 为入口，并把入卷标题 / 提示明确改成终室压测，方便单独校对终房读图、高压大潮与 HUD 节奏
- 近战斩击和玩家投射命中现在也会留下短时的留字残影，让 Godot 版更接近 source 里那种“笔锋划过去后还会挂一层字痕”的战斗反馈
- 战斗里现在也补上了第一批程序化合成音效：书生墨弹、侠客斩击、`休 / 海 / 忍 / 雷 / 炎` 技能、命中反馈、拾取提示、波次推进、字境切换，以及卷主现身 / 退散都会给出独立音色；阵师字阵预警圈、冲锋路线和它们真正起势 / 起爆的瞬间也会给出额外提示音，让高压读招更早一点
- HUD 角落现在会继续显示当前 `战场乐题` 与提示 toast，而且 `苔月幽林 / Mosslight Canopy`、`萤火小径 / Firefly Footpath` 两条 source 风格程序化配乐已经在 Godot 里真正循环播放，并会随着战局压力切换同步更新

### 进度 / 局外系统
- 角色选择界面已经改成“两张角色卡直接二选一”
- 地图迷雾 / fog-of-war 存在
- 地图 modal 已经有缩放、拖拽、图例
- 成语 / 句子类地图事件现在已经接回十二道 `句阵守卫`，并把 `简库中庭 / Slip Archive`、`雷纹内库 / Thunder Vault` 与 `卷渊终室 / Abyss Sanctum` 分成不同句池节奏
- 暂停系统存在，且暂停 / 结算页现在都会继续保留当前源稿构筑路线提醒与构筑进度摘要，不必回到战斗 HUD 才能对照 build 主线
- 死亡结算页可以直接：
  - 重开
  - 回启动器
  - 看排行榜
- 有 `人物志`、`合字图谱`、`怪物图鉴`
- `人物志` 现在除了来路、摘句与角色特性，也会显示对照 web 原型补上的 `攻速 / 拾取` 前台统计，开卷前更容易判断角色手感
- 这些现在都属于 `字海` 的二级菜单层，而不是最外层首页的一部分

### 排行榜
- 当前 Godot 迁移版先落地的是本地排行榜
- 记录保存在本地 cache（`user://local_leaderboard.json`）
- 启动器和 `字海` 二级菜单里都可以打开 `玩家名帖`，为这台设备保存默认排行榜署名
- 从第 1 波真正开卷的正式战绩会进入主卷榜；第 10 / 20 波捷径现在会单独写入试阵榜，不影响主榜排序
- 二级菜单里的本地排行榜现在也支持按波次 / 击破 / 存活排序，更容易从不同角度回看 build 与角色成长结果
- 二级菜单和结算页都可以直接查看最近战绩
- 在线同步榜单还没有重新接回当前仓库主线
- 每局结束自动记录：
  - 波次
  - 击杀
  - 存活时间
  - 用的角色
  - 本机时区与 UTC 偏移
  - 偏旁 / 合字 / 词技 / 神器
  - 各类敌人击倒数
- 玩家可以输入名字
- 如果留空，则会回退到 `玩家名帖` 保存的默认署名；如果还没有自定义名帖，则使用本机生成的武侠名

### 移动端 / 平台适配
`字海` 这边已经做了很多移动端适配：
- 手机 portrait 会被阻止，要求横屏
- 移动端有虚拟摇杆和暂停按钮
- 摇杆已经改成自由位置出现，左右手都能操作
- iPad 识别和摇杆显示问题修过
- 微信内打开的 viewport / UI 混合问题修过
- 提示 toast、失败页滚动、选择页尺寸等都针对手机横屏修过
- 偏旁/神器/磨词选择页在手机上改成居中约 70% 大小的弹窗

### UI / 可读性
- 最近还做过一轮纯 UI 优化，不改玩法，只强化信息分层
- 偏旁、合字、词技、神器不再都是一坨同色同字重
- 战斗桌面 HUD 还整体缩小过约 15%
- 玩家模型头顶现在会常驻一个场内血条，收起 HUD 或在手机上时也能更快看清当前残血压力
- 战斗 HUD 右侧的 `当前目标` 卡现在会额外保留一条源稿构筑路线参考，并按当前 build 进度提示开卷 / 中盘 / 砚台磨词阶段，方便边打边对照 hanziHero 的路线意图
- 同一条源稿构筑路线提醒现在也会进到暂停 / 结算浮层里，附带当前偏旁 / 成字 / 词技数量，移动端或收起 HUD 时也能快速看清这局 build 正写到哪一步
- 当前更强调“重点字、重点类别、重要信息”的视觉层级

## 仓颉之路（以下为迁移前原型记录）
- 以下内容主要保留迁移前 web 原型的玩法与表现记录，方便后续把 `仓颉之路` 迁回 Godot 时继续对照设计目标；当前仓库里它已经有启动器 portal overlay 作为前台预览，但真正的 Godot 爬塔战斗仍待后续接入。

### 核心玩法
- 三路塔图
- 战斗 / 精英 / 事件 / 宝箱 / Boss 流程
- 金币经济、商店、删牌、事件抉择
- `Ink` 作为第二资源
- 不是纯 StS 换皮，已经把汉字组合逻辑深度接进卡牌系统

### 汉字系统
- 类似 `日 + 月 = 明`、`人 + 木 = 休` 这类组合逻辑已经在战斗中体现
- 双语方向明确：不仅给中文母语者玩，也考虑给非中文母语者学习

### 战斗呈现
- 中央 Babylon 3D duel stage
- 浮动伤害 / 护盾 / 治疗 / 能量数字
- 玩家和敌人的状态信息已经做成场内漂浮 3D 面板，而不是传统侧边 HTML 卡
- 手牌移到底部，不塞进 3D 中心舞台
- 手牌现在是简略版，hover / 拖拽时显示更完整的大卡
- 摄像机可以鼠标旋转，但不能缩放
- 舞台灯光已经被调成更明确的两束斜打聚光
- 信息栏增大过，模型缩小过，整体更像舞台 duel 演出

### 最近工程修复
- 首次进入战斗模糊问题修过
- 移动端进不去的报错修过
- 旧浏览器 / 手机兼容做过 fallback
- 默认 avatar glow 降低，并加过 `3D Effects` 控件
- 英文 / 中文双语 UI 骨架已经开始接入

## 工程结构
`项目入口`
- `project.godot`
- `run/main_scene = res://scenes/app/launcher.tscn`

`启动器 / 二级菜单`
- 场景：`scenes/app/launcher.tscn`
- 脚本：`scripts/app/launcher.gd`
- 字海菜单场景：`scenes/app/zihai_menu.tscn`
- 字海菜单脚本：`scripts/app/zihai_menu.gd`

`字海残卷战斗`
- 主战斗场景：`scenes/battle/zihai_battle.tscn`
- 主战斗脚本：`scripts/battle/zihai_battle.gd`
- 实体场景：`scenes/entities/`
- 实体脚本：`scripts/entities/`
- HUD 场景：`scenes/ui/battle_hud.tscn`
- HUD 脚本：`scripts/ui/battle_hud.gd`
- 横屏保护层：`scripts/ui/mobile_landscape_guard.gd`（启动器、字海菜单、战斗场景共用）
- 战斗触控层：`scripts/ui/touch_controls_overlay.gd`

`共享状态 / 基础能力`
- 角色与运行时状态：`scripts/core/session.gd`
- CJK 字体加载：`scripts/core/cjk_font.gd`

`Web 导出`
- 导出脚本：`scripts/export_web.sh`
- 场景 smoke 检查：`scripts/smoke_test_scenes.sh`
- Godot 导出预设：`export_presets.cfg`
- 导出目录：`build/`

## 当前整体判断
- `字海残卷` 已经是一个“核心循环、敌人谱系、世界物件、进度系统、移动端适配、本地排行榜都存在”的完整动作原型
- `仓颉之路` 的迁移前原型已经不是 barebones，而是有自己汉字 identity 的 deckbuilder 方向；当前 Godot 仓库里仍待后续接入
- 整个项目现在最重要的价值，不只是“会显示汉字”，而是“汉字结构和语义已经变成玩法系统”

## 开发与验收分支
- 稳定主干：`main`
- 线上验收分支：`staging`
- 日常开发分支：`develop`
- 历史遗留分支：`test`（暂时保留作回退参考，不再作为默认开发落点）

配套脚本：
- `./scripts/lan-preview.sh`：先导出 Godot Web 产物，再启动本地 / 局域网预览服务
- `./scripts/promote-to-staging.sh`：把当前 `develop` 批次推进到 `staging`
- `./scripts/vercel-ignored-build.sh`：给 Vercel Ignored Build Step 使用，只允许 `staging` 和 `main` 自动部署

如果你只是想在局域网里让别的设备试玩，不需要推远端，直接运行：

```bash
./scripts/lan-preview.sh
```

更完整的日常流程见 [`SOP.md`](./SOP.md)。

## Godot MCP 工作流
- 这个仓库现在默认把 Godot MCP 作为 Codex 和本地自动化的第一交互层。每次开始一轮工作时，先做一次轻量检查，例如读取 Godot 版本或项目信息。
- 如果 Godot MCP 可用，优先用它做项目结构读取、场景/节点操作、Godot 运行态检查和调试输出读取，不再把“只改文件并猜编辑器现场”当成默认方式。
- `./scripts/smoke_test_scenes.sh` 和 `./scripts/export_web.sh` 仍然保留，而且很重要；它们现在主要承担提交前验证、导出验证，以及 Godot MCP 不可用时的保守兜底。
- 如果某次会话或自动化里 Godot MCP 不可用，要显式记录这个 blocker，再退回文件流和无头验证；不要在没有说明的情况下默默降级。

## 当前 Godot 迁移最值得继续保留的核心
优先保留这几条设计主轴：
1. 首页启动器 -> 游戏二级菜单 -> 真正进入战斗 的层级体验
2. `字海` 的偏旁 -> 合字 -> 词技 成长链
3. `字海` 的多类型敌人和明确地面预警
4. `字海` 的地图生态物件（草丛、树、纸、砚、宝箱等）
5. `字海` 的角色差异化，尤其 `侠` 的近战身份
6. `仓颉` 的“字形组合直接进入卡牌构筑”的核心
7. 双语 / 学习向定位

## Vercel 预览部署
- 仓库现在带了 `vercel.json`，构建命令仍然是 `./scripts/export_web.sh`，输出目录仍然是 `build/`。
- 推荐把 Vercel 的 Git 自动部署限制在 `staging` 和 `main`，不要让 `develop` 的每次 push 都触发线上导出。
- 在 Vercel 项目设置里，把 Ignored Build Step 配成：

```bash
./scripts/vercel-ignored-build.sh
```

- 这样 `staging` 用来做线上验收，`main` 用来发正式版本，`develop` 继续承担日常开发和自动化推进。
- 构建命令是 `./scripts/export_web.sh`；实际使用的 Web 导出预设保存在仓库根目录的 `export_presets.cfg` 中。脚本会先导入资源，再导出到 `build/index.html`，所以不需要把 `build/` 产物提交进仓库。
- 本地在导出前可以先跑 `./scripts/smoke_test_scenes.sh`：它会先导入资源，再用独立的临时 Godot runtime 逐个无头启动 `scenes/` 下的场景，尽早发现缺脚本、缺资源或启动时报错。
- 在 Linux / Vercel 环境里，脚本会自动下载 `Godot 4.6.1` 编辑器和 export templates；本地如果已经装了 Godot，则直接复用本机安装。
- 如果需要临时切到别的 Godot 发布号，也可以覆盖脚本内置默认值，例如 `GODOT_VERSION=4.6.1-stable` 与 `GODOT_TEMPLATE_VERSION=4.6.1.stable`；这两个变量需要保持同一版本系。
- 如果本地 Godot 不在默认路径，也可以直接这样跑：`GODOT_BIN=/path/to/Godot ./scripts/smoke_test_scenes.sh` 或 `GODOT_BIN=/path/to/Godot ./scripts/export_web.sh`。如需改导出目录，则可以用 `HANZI_EXPORT_DIR` 或 `HANZI_EXPORT_HTML` 覆盖默认输出位置。
- 本地手动验证也可以直接跑同一条命令：`./scripts/export_web.sh`
- 如果你想直接给同一局域网里的设备试玩，用 `./scripts/lan-preview.sh` 即可；默认会先导出，再在 `0.0.0.0` 上启动 HTTP 服务并打印局域网地址。
