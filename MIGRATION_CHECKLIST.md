# Godot Migration Checklist

Last refreshed: 2026-03-24

Status legend:
- `[done]` already matches or has a solid Godot replacement
- `[in progress]` started in Godot but still needs follow-through
- `[pending]` identified gap that can be migrated safely later
- `[blocked]` should wait for a larger foundation or repo decision

## Launcher

- `[done]` Godot launcher home with two game cards, floating glyph ambience, and mobile browser guidance. Notes: hanziHero uses a launcher homepage with portal cards; Godot now preserves the same top-level game-selection role.
- `[done]` About overlay upgraded to a richer story/article layout with two game summaries and migration notes. Notes: hanziHero's launcher has an about portal with story paragraphs, game cards, and note cards; Godot now carries that structure in-code.
- `[done]` Theme toggle parity for launcher and about surface. Notes: hanziHero supports a paper-ink theme switch; Godot launcher and about overlay now expose a remembered `夜墨 / 纸墨` toggle while preserving the existing night-ink default.
- `[done]` Launcher-side latest update spotlight. Notes: hanziHero's homepage surfaces a recent version / changelog spotlight; Godot launcher now mirrors that front-page role with an in-launcher migration progress card.
- `[done]` Launcher-side changelog history overlay. Notes: the source launcher can open a dedicated changelog panel; Godot launcher now exposes a matching in-launcher update-history overlay from the latest-update card instead of stopping at a single snapshot.
- `[done]` Bilingual launcher copy and runtime language toggle. Notes: hanziHero ships zh/en launcher strings; Godot launcher now remembers a runtime `中 / EN` toggle, translates the main portal/front-page copy, and carries the preference across rebuilds.
- `[done]` Cangjie portal reward and post-battle relic preview. Notes: the source tower flow chains Gold + card rewards after fights and 2-to-3 relic picks after elite / boss clears; Godot launcher now mirrors that with a dedicated `战后抉择 / Post-Battle Flow` section so the portal explains how runs keep bending after each win.
- `[done]` Cangjie portal duel-stage preview interaction. Notes: the source `仓颉之路` portal lets players click the staged duelists and quickly trim stage FX; Godot now mirrors that inside `起笔登塔 / Start Climb` with a lightweight tap-reactive faceoff panel and a local `3D 特效 / 3D Effects` toggle, without pretending the full card battle is already ported.
- `[in progress]` Cangjie portal follow-through. Notes: Godot launcher now gives `仓颉之路` a dedicated portal overlay that previews the source-style `Start Climb`, `Run Controls`, `Card Codex`, `Fusion Atlas`, `Relic Shelf`, `Post-Battle Flow`, and `Tower Guide`, plus a small duel-stage response beat in the entry tab, a matching preview of the source page's always-visible `Restart Run / Open Deck / Back To Launcher` strip with `Floor / HP / Deck / Relics / Gold` status framing, and a compact three-lane tower-route shell that mirrors the source page's climb framing without pretending the real node logic is already playable; a true playable Godot scene or direct prototype hand-off is still pending.

## Menus

- `[done]` Zihai second-layer menu with hero selection and archive overlays. Notes: Godot has dedicated overlays for 人物志、合字图谱、怪物图鉴、本地排行榜, matching the source menu depth.
- `[done]` Character selection flow with scholar / xia split and direct battle entry. Notes: source menu moved to "start -> choose hero -> battle"; Godot keeps that same hierarchy.
- `[done]` Quick-start wave shortcuts for menu-side test entry. Notes: hanziHero keeps dedicated test starts for wave 10 / 20; Godot now exposes equivalent menu buttons and routes those shortcut runs into a separate local test board.
- `[done]` Character reaction flavor on selection. Notes: hanziHero role cards trigger short personality lines; Godot now rotates short in-character response quotes in the detail panel whenever a hero is picked.
- `[done]` Character archive lore and trait dossier. Notes: hanziHero's 人物志 surfaces role background, quoted thread, and role-trait guidance; Godot now mirrors that in the menu detail panel and 人物志 copy instead of stopping at flat combat stats.
- `[done]` Source hero-lore stat readout parity in menu and archive. Notes: hanziHero's lore cards expose attack cadence and pickup coverage alongside the base role profile; Godot now surfaces `攻速 / 拾取` next to the existing mobility, health, damage, and range readout in both the main dossier and 人物志.
- `[done]` Inline hero-card reaction bubble and selected state. Notes: the source character cards show a selected chip plus the tapped hero's quote inline; Godot now mirrors that on the left-side hero cards instead of keeping reactions only in the dossier panel.
- `[done]` Player Sigil default-name flow in launcher and menu. Notes: hanziHero lets the launcher / portal maintain a device signature reused by leaderboard entries; Godot now mirrors that with a persistent `玩家名帖` overlay backed by `user://leaderboard_identity.json`.
- `[done]` Theme toggle parity inside the menu layer. Notes: source launcher/menu surfaces share the remembered paper-ink theme; Godot Zihai menu and its overlays now inherit the same `夜墨 / 纸墨` preference and expose a matching top-bar toggle.
- `[done]` Language toggle parity inside the menu layer. Notes: the Godot Zihai menu now remembers the same `中 / EN` preference, exposes a working runtime toggle, and translates the hero/front-end menu flow instead of keeping a dead placeholder pill.
- `[done]` Menu-side leaderboard build snapshots. Notes: hanziHero's menu leaderboard cards surface build lanes and kill mix; Godot menu leaderboard now appends `偏旁 / 成字 / 词技 / 击倒` 摘要 under each entry instead of stopping at flat score lines.
- `[done]` Menu-side leaderboard sorting controls. Notes: hanziHero's source leaderboard can pivot between wave, kills, and survival-time ordering; Godot now mirrors that in the menu overlay while also surfacing level inline with each run summary.
- `[done]` Local leaderboard logged-date metadata. Notes: hanziHero's leaderboard cards show when each run was logged; Godot menu and result-side local leaderboard views now include a `记录于` line ahead of the existing time-zone metadata so run reviews are easier to place in time.
- `[done]` Menu-side leaderboard back-to-top control. Notes: hanziHero's leaderboard panel exposes a quick `返回顶部 / Back to Top` affordance once the list is scrolled deeper; Godot menu leaderboard now mirrors that with the same delayed scroll-top action instead of trapping longer local boards at the bottom.
- `[done]` Source build-route preview surfaced in menu and archive. Notes: hanziHero's web front-end frames route-choice archetypes like `墨守流 / 雷阵流 / 游侠流 / 烈笔流`; Godot now mirrors that with a compact primary `源稿构筑方向` preview in the main current-writer spotlight and fuller paired route cards in 人物志, while clearly marking them as front-end guidance rather than actual runtime route bias.
- `[done]` Hero-specific menu-to-battle handoff card. Notes: the source front end keeps the chosen writer centered through the final click-to-run beat; Godot's run-start transition now keeps the selected hero's accent, opening-radical tags, focus line, and primary source-route cue instead of dropping back to a generic entry card, and the same overlay now also uses the correct quick-start chapter title for wave 10 / 20 entries.
- `[in progress]` More menu-side build and progression surfacing beyond leaderboard snapshots. Notes: Godot now combines the richer hero dossier with compact `起笔落点` cues, a menu-side `源稿字技` preview, a main-spotlight source route hint, compact `残卷路线` progression cards in the menu detail panel, fuller paired `源稿遗物 / 词技` route cards in 人物志, inline hero-card reactions/selected state, source-style leaderboard sort toggles, a remembered zh/en front-end toggle, and a matching battle-side route reminder; true route-bias mechanics still remain.

## Battle HUD

- `[done]` Pause overlay, restart / return actions, and local leaderboard access. Notes: hanziHero exposes pause plus result-side leaderboard views; Godot now keeps the active source route reminder, progression stage, and current build counts visible in both pause and result overlays while preserving post-run naming/editing for local records.
- `[done]` In-world hero health bar above the player model. Notes: hanziHero keeps survival state visible above the actor so low HUD density or mobile play does not hide danger; Godot now mirrors that with a persistent floating health bar over the current hero while keeping the existing corner HUD bars.
- `[done]` Map modal with fog-of-war, drag, zoom, legend, and exploration summary. Notes: source has a modal map with zoom/drag/legend; Godot now has a close equivalent.
- `[done]` Mobile joystick, touch interact, pause button, and landscape guard. Notes: hanziHero mobile battle flow depends on these protections; Godot already has matching control coverage.
- `[done]` First runtime settings / LOD panel slice. Notes: Godot pause overlay now exposes a low-risk `战场布置` panel with remembered performance preset, enemy health bar toggle, and ambient glyph density controls, covering the first practical slice of hanziHero's settings modal.
- `[done]` Runtime settings follow-through for effect/detail parity. Notes: Godot pause overlay now remembers separate `视觉字效` and `远敌细节` toggles alongside the original preset slice, so decorative glyph bursts can be trimmed independently and distant enemy badges / health bars collapse back to a near-only read when detail is lowered.
- `[done]` Music track toast and soundtrack UI. Notes: hanziHero shows the active procedural track name and mood; Godot battle HUD now carries a matching `战场乐题` card plus a top-right `配乐提示` toast that reuses the source track names and mood text on key battle beats.
- `[done]` Phrase / event log panel. Notes: Godot battle HUD now keeps a dedicated `战报` panel that records wave pushes, realm shifts, boss beats, recipe/word upgrades, and pickup highlights, with a compact mobile-safe variant for smaller screens.
- `[done]` Bilingual HUD copy. Notes: source battle UI can switch zh/en; Godot battle HUD, pause/result overlays, map labels, soundtrack card, and key battle-side banner/tip copy now follow the remembered `中 / EN` runtime language toggle.
- `[done]` Battle-side source build-route reminder. Notes: hanziHero keeps the current route visible in the battle-side build context; Godot now mirrors that in the HUD objective card and compact summary by surfacing the closest source route, current progression stage, and matching tags without introducing live route-bias mechanics.
- `[done]` Battle-start writer identity reveal and HUD carry-through. Notes: the source run start keeps the selected writer's identity visible beyond the last menu click; Godot now opens battle with a centered reveal that repeats the current writer title, quote source, primary route seal, and opener state, the in-battle intro panel keeps a compact `印记 / 路印` line using each hero's localized trait label and primary source route after that reveal fades, and the first `战场呼应` intro card now also carries a compact writer-mark / route-seal strapline plus the quote source so that identity survives into the first live combat response.
- `[done]` Pause/result route-focus recap. Notes: hanziHero's pause summary keeps the chosen build lane visible alongside the current build context; Godot pause and game-over overlays now repeat the closest source route, current progression stage, and current `偏旁 / 成字 / 词技` counts so the run can be reviewed without reopening the live HUD card.

## Combat Systems

- `[done]` Auto-attack survival loop with radical draft, recipe formation, and inkstone word grinding. Notes: source progression is "偏旁 -> 成字 -> 词技"; Godot already preserves that core loop.
- `[done]` Two-role hero split between ranged scholar and melee xia. Notes: hanziHero gives xia a close-range sword identity; Godot mirrors that distinction.
- `[done]` Enemy roster baseline with clear telegraphs. Notes: Godot already includes basic, swift, tank, archer, assassin, cavalry, ritualist, elite, and boss enemies with warning zones.
- `[done]` World-prop baseline for trees, bushes, inkstones, chests, stelae, scroll racks, and ink pools. Notes: the source battlefield is no longer an empty field; Godot already supports a comparable landmark layer.
- `[done]` First guarded phrase event in `简库中庭 / Slip Archive`. Notes: Godot now plants a visible `风雨同舟` phrase stele in the first deeper chamber; walking into it reveals a dedicated guardian and defeating that guardian grants the first sentence-style room reward.
- `[done]` Second guarded phrase event with a non-heal reward. Notes: `简库中庭 / Slip Archive` now also carries `海上生明月`; its ranged guardian pays out extra `字墨` instead of only repeating the first heal-only sentence reward.
- `[done]` Third guarded phrase event with a reveal reward. Notes: `简库中庭 / Slip Archive` now also carries `山高水长`; its tank guardian widens nearby map reveal so sentence rewards cover heal, `字墨`, and fog-expansion beats.
- `[done]` Fourth guarded phrase event with a radical reward. Notes: `简库中庭 / Slip Archive` now also carries `举头望明月`; defeating its moonward guardian grants a fixed `月` radical so sentence rewards now span heal, `字墨`, reveal, and radical outcomes.
- `[in progress]` Phrase / idiom guardian encounters. Notes: `简库中庭 / Slip Archive` keeps the first four reward-type-complete guarded phrases, and `雷纹内库 / Thunder Vault` now adds `风驰电掣`、`电光石火`、`云收雨霁`、`雷霆万钧`, so a later chamber now also covers heal / `字墨` / reveal / fixed-radical rewards while the final-chamber phrase pass still needs follow-through.
- `[pending]` Relic / artifact system. Notes: hanziHero treats relics as a second growth lane parallel to radicals; Godot chests currently drop only direct pickups.
- `[done]` Stage phase-shift themes. Notes: Godot now rotates every 4 waves through `碑林 / 墨潮 / 雷纹 / 残卷`, blends fog/ground/backdrop mood, and stamps giant lingering hanzi near the player when the realm shifts.
- `[in progress]` Wider radical / recipe / word pool. Notes: Godot now carries 明 / 休 / 海 / 雷 / 岩 / 忍 / 炎, including the new source-style `石 + 山 -> 岩` impact branch and the first safe pass on recipes that currently stop at glyph level instead of forcing a fake phrase upgrade; the source prototype still has broader branches such as 双日、林、沁、囚、俊、强 and later phrase follow-through waiting to migrate.
- `[done]` Static `聚墨符 / 疾书令` battlefield pickups. Notes: hanziHero ships one-shot utility pickups for full-map ink recall and burst haste; Godot now has fixed battlefield placements with matching core effects.
- `[done]` Enemy-dropped utility pickup routing and `回春丹` parity. Notes: Godot elites / bosses can now seed `聚墨符 / 疾书令`, regular enemies can occasionally route those pickups back into the field, and a `回春丹` drop meter restores the source-style max-health recovery pickup instead of relying on static-only utility spawns.

## Progression

- `[done]` Local leaderboard persistence with player naming, hero, bosses, kills, radicals, recipes, words, and enemy counts. Notes: hanziHero already records local runs; Godot mirrors that in `user://local_leaderboard.json`.
- `[done]` Leaderboard time-zone metadata in local run records. Notes: the source web prototype keeps each run's time zone and UTC offset for cross-region testing; Godot now saves the system zone/offset with each local record and surfaces it in both menu-side and result-side leaderboard views.
- `[done]` Word-grind gating at the inkstone rather than passive auto-unlock. Notes: the source moved word skills behind an explicit station interaction; Godot follows that rule.
- `[done]` Dedicated test-run leaderboard view. Notes: hanziHero keeps separate main/test boards; Godot now records wave 10 / 20 shortcuts into a dedicated `试阵榜` while keeping wave 1 runs on the main board.
- `[pending]` Cloud leaderboard sync. Notes: hanziHero has online leaderboard plumbing; Godot only keeps local records today.
- `[pending]` Relic-aware build summary. Notes: source pause / result views include owned relic context; Godot cannot yet show that lane because relics are not ported.
- `[done]` More map-event reward routing. Notes: Godot now carries heal, `字墨`, map-reveal, and fixed-radical guarded-phrase rewards in `简库中庭 / Slip Archive`, matching the current source reward spread for guarded phrase events.
- `[done]` First boss-clear chamber-break continue gate. Notes: hanziHero pauses before sending the run into the next room; Godot now stops after the first scroll-lord clear once the remaining field is quiet, shows a dedicated `卷间缓冲` overlay, and requires an explicit continue before combat resumes.
- `[done]` First chamber-break reward / event / recovery trio. Notes: Godot now turns the first `卷间缓冲` stop into a real `奖励 / 异事 / 修整` pick, with a carried radical supply, a low-risk scroll-echo branch while relic routing is still missing, and a short-rest recovery option before combat resumes.
- `[done]` Chamber-break next-room preview block. Notes: hanziHero's interlude card previews the upcoming room's wave, realm, pressure, and likely threat mix; Godot now mirrors that with a dedicated `下一段预览` block on the first `卷间抉择` overlay before the reward / event / recovery choice resolves.
- `[done]` Dedicated chamber-advance overlay after the interlude pick. Notes: hanziHero pauses one more time before the actual room swap; Godot now mirrors that with a `续卷入深层 / Continue Deeper` state that re-shows the next-room preview after the reward / event / recovery choice has been sealed.
- `[done]` First carried chamber modifier pass for interlude picks. Notes: Godot's first `卷间抉择` now leaves a lightweight modifier until the next scroll lord falls: reward raises paper / seal supply drops, `残卷回响` biases pressure-enemy drops toward extra `残纸` and lets elites echo a `疾书令`, and `歇笔回气` echoes smaller heal / brush resets on later wave pushes.
- `[done]` First interlude-driven radical draft lean. Notes: the first `卷间抉择` now also nudges later level-up radical drafts through the next chamber: `简库拓片` leans toward the carried radicals themselves, `封钥借契` tilts toward `木 / 氵 / 田 / 月`, and `守灯静读` tilts toward `亻 / 心 / 木 / 月` until the next scroll lord.
- `[done]` First named deeper chamber swap and room reseed. Notes: after the first `卷间抉择`, Godot now actually pushes the run into a `简库中庭 / Slip Archive` chamber with rebuilt trees, bushes, racks, stelae, inkstones, chests, utility pickups, and fresh fog-of-war instead of reusing the opening room unchanged.
- `[done]` Second chamber-routing follow-through into `雷纹内库 / Thunder Vault`. Notes: `简库中庭 / Slip Archive` now also uses a source-style `雷纹转钥封 / Storm Latch Seal` gatekeeper beat before the reward beacon rises, and its next interlude swaps into a second chamber-specific trio (`雷纹拓笔 / 伏雷换契 / 伏纹稳息`) before reseeding the run into a colder `Thunder Vault` layout with its own prop rhythm.
- `[done]` Third chamber-routing follow-through into `卷渊终室 / Abyss Sanctum`. Notes: `雷纹内库 / Thunder Vault` now feeds into a final-room interlude trio (`终室备墨 / 渊页誓约 / 压关静息`) before reseeding the battle into a darker `Abyss Sanctum` shell, and chapter completion now waits for one more scroll-lord timing so the sanctum is a real playable last chamber instead of only a checklist placeholder.
- `[done]` Abyss quick-start follow-through. Notes: the wave-20 stress-test entry now explicitly targets `卷渊终室 / Abyss Sanctum`, updates its intro copy to describe the final-room pressure pass, and keeps late-start health syncing safe before the HUD has finished spawning.
- `[done]` First post-boss room objective beyond wave clear. Notes: Godot now mirrors the source's newer sealed-exit cadence in `入卷前庭 / Entry Court`: the room can now branch into either the named `砚门守魁 / Ink Gate Warden` ward path or a two-seal relay route, and only after that active objective is resolved does the actual `奖` reward beacon rise for the between-chambers choice.
- `[done]` Chamber-end reward beacon before interlude choices. Notes: hanziHero makes the player walk to a visible `奖 / LOOT` beacon before the between-room reward / event / recovery choice resolves; Godot now does the same after a room is truly clear instead of opening the interlude from anywhere on the map.
- `[done]` Chamber objective guidance chip for seals and reward beacons. Notes: hanziHero keeps a live `奖 / 封` travel pointer during room-exit beats; Godot now pins a screen-edge guide to the nearest active seal or chamber reward beacon so the first chamber break no longer relies only on HUD text memory.
- `[in progress]` Dungeon-room transition and interlude flow. Notes: hanziHero now breaks longer runs into room-to-room reward / event / recovery beats after major milestones; Godot now has the first chamber-break continue gate, chamber-end reward beacons, live seal / reward guidance, source-style next-room preview / continue-deeper overlays, the first carried chamber-modifier pass, a first interlude-driven radical draft lean, three real named chamber reseeds (`简库中庭 / Slip Archive`、`雷纹内库 / Thunder Vault`、`卷渊终室 / Abyss Sanctum`), source-style seal/gatekeeper exits in both `入卷前庭 / Entry Court` and `Slip Archive`, eight guarded phrase events across two chambers, and three chamber-specific interlude trios (`简库拓片 / 封钥借契 / 守灯静读`、`雷纹拓笔 / 伏雷换契 / 伏纹稳息`、`终室备墨 / 渊页誓约 / 压关静息`), but relic-backed event outcomes and a final-chamber phrase pass are still missing.

## Content

- `[done]` Character archive, recipe atlas, and enemy codex text live in shared session data. Notes: source uses compendium-style front panels; Godot already ships equivalent text-driven overlays.
- `[done]` Battlefield hero callouts and elite/boss taunt beats. Notes: hanziHero surfaces reactive frontline lines and enemy taunts in battle; Godot now mirrors that with a dedicated `战场呼应` card plus event-log entries for hero intro / recovery / milestone quotes and elite / boss entrance taunts.
- `[done]` Menu/archive hero lore follow-through. Notes: Godot人物志 now mirrors the source portal more closely with richer lore cards, archive excerpts/sources, and actual opening-radical cues wired into both the menu detail panel and battle start state.
- `[done]` Active writer showcase tap response. Notes: the Godot menu now mirrors the source “card or current stage” interaction more closely by letting the current-writer showcase replay the selected hero response and reuse the existing front-end pulse when that stage is tapped.
- `[in progress]` More front-end hero flavor beyond the archive pass. Notes: Godot now mirrors the source card-side quote bubble, selected marker, source-style scholar/xia avatar silhouettes, hero-accented current-writer preview tinting, a short selection pulse, a quoted-thread caption / stage shell, a hero-specific battle-entry handoff card, and a matching first in-battle writer reveal across the menu spotlight, hero cards, archive panels, run-start transition, and battle opening alongside the existing opening cues / source-skill preview / primary route hint strip and battle-side callouts, but there are still extra front-portal framing beats that have not been ported.
- `[done]` More narrative and educational copy parity from the launcher / about surface. Notes: Godot about overlay now mirrors the source launcher more closely with explicit audience, shareability, migration-focus, and next-product-step cards instead of stopping at a generic migration note.
- `[blocked]` Full `仓颉之路` Godot port. Notes: the web prototype is playable, but the Godot repo does not yet have the deckbuilder combat/map foundation needed for a safe direct migration.

## Polish

- `[done]` Tree fade-through, bush anti-abuse lockout, banners, and strong telegraph readability. Notes: Godot already carries several of the source combat-polish beats into 3D.
- `[done]` Lingering slash and hit afterimages. Notes: hanziHero lets melee cuts and combat hits leave short-lived glyph echoes in the air; Godot now adds extra `刂 / 斩` slash trails for close-range swings plus small player-impact afterimages so hits no longer read as only one-frame rings.
- `[done]` First procedural combat SFX slice for battle parity. Notes: Godot battle now synthesizes distinct scholar-shot / sword-slash cues, separate `休 / 海 / 忍 / 雷 / 炎` skill hits, enemy impact thuds, pickup chimes, and wave / realm-shift / boss stingers through `scripts/core/battle_audio.gd`, closing the silent-combat gap without shipping external audio assets.
- `[done]` Procedural music playback and current-track feedback. Notes: Godot battle now synthesizes and loops the source `苔月幽林 / Mosslight Canopy` and `萤火小径 / Firefly Footpath` tracks inside `battle_audio.gd`, while the existing `战场乐题` card and `配乐提示` toast stay in sync when battle pressure swaps tracks.
- `[in progress]` Higher-end hit/audio differentiation follow-through. Notes: the synth pass now covers attacks, impacts, pickups, marquee battle beats, ground-telegraph / charge-warning cues and their activation blooms, a distinct player-collapse cue, and enemy-family-specific hurt / defeat signatures, but broader word-skill layering and richer hero-specific hurt stingers still remain.
- `[done]` Themeable launcher/menu presentation. Notes: Godot launcher, about overlay, and Zihai menu now share the remembered `夜墨 / 纸墨` presentation; battle/HUD localization and broader downstream parity remain tracked separately.
- `[in progress]` More stage spectacle for big unlocks and transitions. Notes: Godot battle HUD now adds larger centered reveal cards for new glyphs, phrase arts, `字境相变`, boss arrivals, chapter-secured beats, a source-style `续卷入深层 / Continue Deeper` chamber advance state, and active chamber-objective guidance, but later chamber transitions and other flourishes can still be pushed further.

## Export

- `[done]` Godot Web export script, export presets, and Vercel deployment path. Notes: the Godot repo already has a direct replacement for the web repo's static hosting flow.
- `[done]` Root README documentation for export overrides and deployment expectations. Notes: recent README maintenance now matches the current `scripts/export_web.sh` behavior.
- `[done]` Automated scene smoke checks in repo scripts. Notes: `scripts/smoke_test_scenes.sh` now imports assets, runs every `scenes/*.tscn` headlessly with an isolated temp runtime/log path, and catches startup-time errors before export.

## Technical Debt

- `[done]` Long-lived migration tracking in repo root. Notes: `MIGRATION_CHECKLIST.md` remains the source of truth and now has a matching long-lived GitHub tracker issue for checklist/body sync instead of one-off progress issues.
- `[done]` Shared pickup and chest battlefield copy. Notes: hanziHero's source web data keeps pickup/chest toast wording in shared bilingual content; Godot now routes paper / ink / seal / utility pickup plus chest banner-tip-log copy through `scripts/core/front_end_content.gd` instead of hardcoding those strings in `scripts/battle/zihai_battle.gd`.
- `[in progress]` Shared localization layer for launcher, menu, and battle UI. Notes: shared hero, radical, recipe, word, enemy, soundtrack, intro-title, and cue translation data now live in `scripts/core/hanzi_localization.gd`; `scripts/core/front_end_content.gd` now also feeds the bilingual local-leaderboard/result-overlay copy, the pause / game-over / runtime-settings overlay titles plus summary/setting formats, the battle-HUD shell English map plus compact-tip/callout/objective placeholder copy reused by `scripts/ui/battle_hud.gd`, the battle-HUD runtime health / progress / status lines, radical stock summaries, route-progress summary, skill badges, compact-skill placeholders, route intro/callout identity lines, empty-skill placeholder copy, boss labels, and radical / inkstone choice prompts reused by `scripts/ui/battle_hud.gd`, the chamber-interlude reward / event / recovery banner-tip-log copy reused by `scripts/battle/zihai_battle.gd`, the live pickup / chest banner-tip-log copy reused by `scripts/battle/zihai_battle.gd`, the default battle loop / inkstone helper tips plus inkstone / phrase-refine choice and progress copy reused by `scripts/battle/zihai_battle.gd`, the phrase-event reward/guardian copy, guardian fallback labels, stela badge text, boss-defeat detail, chamber-transition body, chamber-preview line/title shell, chamber-shift banner/reveal/log copy, short-rest echo banner/log copy, chamber-interlude overview paragraphs, live wave-pressure tips, chamber names/tips, chamber-exit seal / gatekeeper names/descriptions/taunts, the general hero/enemy callout shell text plus source-style enemy entrance taunt pools, and field-phase names / cues / tips plus reveal/banner shell copy reused by `scripts/battle/zihai_battle.gd`, while broader battle event text still remains inline in scene scripts.
- `[in progress]` Move remaining battle tip/callout body copy into shared content. Notes: phrase-event reward copy, chamber-transition body, chamber-preview shell labels, chamber-transition shell copy, boss / wave guidance, chamber names/tips, chamber-exit seal / gatekeeper descriptive copy and taunts, field-phase naming/cue/tip plus reveal/banner shell copy, the battle-HUD callout/objective shell placeholders, runtime status / choice prompts, route intro / empty-skill fallback copy, the inkstone / phrase-refine choice headlines plus refine warning/progress banners, and now the chamber-interlude option labels, elite/boss arrival plus chapter-resolution battle-state copy, the general hero/enemy callout shell copy, enemy entrance taunt pools, boss-stage / test-jump battle-state copy, plus defeat/restart summary copy reused by `scripts/battle/zihai_battle.gd` all route through `scripts/core/front_end_content.gd`, but some map summary strings and other battle text still remains embedded in `scripts/battle/zihai_battle.gd`.
- `[pending]` Break up `scripts/battle/zihai_battle.gd` into smaller systems. Notes: source JS has already started splitting combat/rendering/data concerns; Godot battle logic is still concentrated in one large script.
- `[in progress]` Move launcher / menu content into data-driven definitions. Notes: `scripts/core/front_end_content.gd` now drives launcher cards, launcher about/profile/changelog copy, Cangjie portal sections, menu shell copy, menu top actions, menu quick-start entries, menu main-detail hints, the shared menu English localization map, menu-side stat labels and value formats, menu-side theme/language toggle copy, menu overlay headers/summaries/CTA copy, menu-side transition shell and runtime transition copy, the first batch of archive section labels/notes, archive-side opening-route and source-skill fallback copy, archive-side starting-radical summaries/tags, active-skill cooldown plus headline formatting, archive stat labels and quote/title/source formats, archive background/source labels, build-route pairing titles and hint formatting, menu-side selection-state and profile-status copy, recipe-intro plus atlas line formats, leaderboard summary/empty-state/button copy, leaderboard record-line formats, leaderboard identity/detail join/count formats, leaderboard recorded-on/time-zone formatting, leaderboard count-entry and enemy-kill summary formats, enemy-codex intro/entry/summary labels and enemy-codex detail labels; battle-side local leaderboard/result titles, alias prompts, board-switch copy, pause/game-over/settings overlay titles, runtime setting labels, and another battle-HUD shell slice for the callout/objective placeholders, reveal card defaults, event-log placeholder, and map modal legend/help/button copy now also reuse that shared content, while deeper archive card bodies and broader live HUD/event wording still remain inline in scene scripts.
