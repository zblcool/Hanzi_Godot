prefix_rule(
    pattern = ["./scripts/gh-bot", ["issue", "api", "auth"]],
    decision = "allow",
    justification = "Use the repository bot wrapper for GitHub operations in Hanzi_Godot.",
    match = [
        "./scripts/gh-bot issue list --repo zblcool/Hanzi_Godot --state open --limit 1",
        "./scripts/gh-bot api graphql -f query=query",
        "./scripts/gh-bot auth status",
    ],
    not_match = [
        "./.tools/gh/bin/gh issue list --repo zblcool/Hanzi_Godot --state open --limit 1",
        "gh issue list --repo zblcool/Hanzi_Godot --state open --limit 1",
    ],
)

prefix_rule(
    pattern = ["./.tools/gh/bin/gh", ["issue", "api", "auth"]],
    decision = "forbidden",
    justification = "Use `./scripts/gh-bot` so GitHub issue operations use the bot account and the stable wrapper path.",
    match = [
        "./.tools/gh/bin/gh issue list --repo zblcool/Hanzi_Godot --state open --limit 1",
        "./.tools/gh/bin/gh api graphql -f query=query",
        "./.tools/gh/bin/gh auth status",
    ],
)

prefix_rule(
    pattern = ["gh", ["issue", "api", "auth"]],
    decision = "forbidden",
    justification = "Use `./scripts/gh-bot` instead of bare `gh` in this repository.",
    match = [
        "gh issue list --repo zblcool/Hanzi_Godot --state open --limit 1",
        "gh api graphql -f query=query",
        "gh auth status",
    ],
)
